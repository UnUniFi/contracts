# Swap For Bridge

This contract is for swapping tokens of IBC denoms that have same origin denom.
This contract is assumed to be used via IBC Hooks module. By doing so, ICS20 transfer can behave like "Liquidity Bridge".
